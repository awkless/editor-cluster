[settings]
description = "Text editor configuration cluster"
remote = { url = "https://github.com/awkles/editor-cluster.git" }
work_tree_alias = "$HOME"

[[dependency]]
name = "shell"
remote = { url = "https://github.com/awkless/shell-cluster.git" }
